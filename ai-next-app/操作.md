# Next项目

## 创建next项目：

默认创建：

```
pnpm create next-app@latest 
```

个人习惯（推荐使用）：使用 `--src-dir` 时，Next.js 会把应用放在 `src/app` 中，并将 `@/*` 别名配置为指向 `./src/*` 

```
pnpm create next-app@latest --src-dir
```

